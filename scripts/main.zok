import "hashes/sha256/512bitPacked" as sha256packed;

def main(private field a, private field b, private field c, private field d, field[2] hash) -> bool {
    // field[2] h = sha256packed([a, b, c, d]);
    field[2] computed_hash = sha256packed([a, b, c, d]);
    field[2] computed_hash_with_nonce = sha256packed([a, b, c, d]);
    return (computed_hash[0] == hash[0] && computed_hash[1] == hash[1];
}